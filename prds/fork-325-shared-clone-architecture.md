# PRD fork#325 — Should concurrent orchestrations share one git clone at all?

**Issue:** [prageethw/dot-agent-deck#325](https://github.com/prageethw/dot-agent-deck/issues/325)
**Priority:** High
**Status:** M1-M6 shipped. M1 decided 2026-08-18, **corrected same day** after implementation-prep investigation found the originally-approved trigger ("human vs. automated") doesn't match the actual incident shape or any existing code signal; see "M1 correction" below. M2's fork#166 amendment followed up 2026-08-20 to describe the actual shipped Nth-concurrent trigger, once M3-M5 confirmed what that trigger actually is. M3 shipped (Model A: PR #481; Model B: PR #504, issue #490); a gap in M3's own default (blank-slug) path was found and closed afterward as issue #489/PR #506, adding the `SiblingScope::ExactCwdOnly`/`AnySharedCommonDir` split (`orchestration/worktree/017`) — a blank-slug spawn colliding with a live sibling is refused, not isolated. M4a (isolated-clone discovery) shipped: PR #510. M4b (forgery-cost/coverage residuals) shipped: PR #515 — closed a self-inflicted blocker along the way (see M4b's own bullet below), plus filed #516 for two new Low-severity residuals found in final review. M4c (auto-reclaim eligibility) is maintainer-decided and shipped in three rounds: round 1 compared only the clone's commit SHA against the PR's merge-commit SHA; round 2 — after an audit found 3 blocker-severity gaps in round 1 (a forged/unowned clone could satisfy it with no deck involvement, a dirty tree wasn't consulted, and a squash-merge repo's commit SHAs essentially never match a genuine clone at all) — tightened to the AND of `has_attach_lock`, a clean tree, exactly one local branch, an empty stash list, and HEAD-**tree**-SHA (not commit-SHA) equality with the merged PR's merge-commit tree; round 3 (PR #526, reviewer B2) replaced that fifth condition entirely after live measurement against this very repo showed a genuine clone's own HEAD is (almost) never equal to `mergeCommit.oid` under any GitHub merge strategy (PR #481 head `7339edd5f440` vs merge `1ceb919349ef`; PR #477 head `11d6327f2421` vs merge `5742ad1f93dd`), so round 2's rule could (almost) never fire for a real clone — the fifth condition is now plain HEAD-**commit**-SHA equality against the merged PR's own `headRefOid` (a flat `gh pr list --json` field, needing no `gh api graphql` round trip at all), and round 3 also added a full TOCTOU re-verification immediately before deletion (reviewer M1) and an explicitly documented liveness residual (M2 — no signal reachable from `worktree reclaim`'s CLI-subprocess context, which never connects to the daemon). See this bullet's own text below and `docs/develop/shared-clone-architecture.md` for the shipped rule's exact shape. M5 (the actual 3+-orchestration incident-shape test) shipped: PR #517. M6 (docs) shipped: `docs/develop/shared-clone-architecture.md`.
**Predecessors:** PR #331 (attach-race lock, shallow-clone detection), PR #420 (repository-state preflight), PR #458 / PR #471 (worktree-removal attribution + liveness gate) — all three already-merged directions of #325; this PRD covers only the fourth, deliberately deferred by all three: *"Consider whether concurrent orchestrations should share one clone at all."*
**Related:** PRD fork#166 (agent-provisioned worktrees — the PRD that made today's single-shared-clone model **explicit and intended**), PRD fork#175 (delegate provisions the worktree — unimplemented, assumes single-clone), PRD fork#298 (worktree owner human/agent), PRD #236 (worktree removal safety — already notes #120 and #220 use two different, unreconciled clone strategies)
**Fork-only?** No — the underlying provisioning code (`dispatch.rs`, `issue_dispatch_run.rs`, `ui.rs`'s spawn path) is upstream code. Offer upstream per rule 19 once a direction is decided and shipped.

## Problem Statement

Issue #325's original incident was two failures from concurrent orchestrations sharing one `.git` object store and one worktree registry: an orchestration deleted worktrees it didn't own mid-use, and a shallow fetch broke merges across every worktree on the shared repo. The first three of the issue's four "directions" are fixed — a shallow-repo preflight (PR #420), an attach-race lock (PR #331), and removal attribution plus a liveness gate for the one production removal path that lacked one (PR #458/#471). All three explicitly declined to touch the fourth: *"Consider whether concurrent orchestrations should share one clone at all... the strongest version of this is that they shouldn't — but that trades against build-cache sharing and disk, so it's a real design question."*

That framing needs correcting before a decision can be made on it, on two counts this PRD's investigation surfaced.

### Correction 1 — "share one clone" is not this repo's actual behavior; three different models already coexist

- **Model A — TUI-initiated orchestration** (`src/ui.rs:10214`, PRD fork#166): every orchestration is provisioned as a `git worktree add` sibling off **the deck's own already-open root checkout**. PRD fork#166 states this outright: *"Every orchestration starts from the deck's own checkout (`main`). **Intended** — not something to prevent"* (`prds/done/fork-166-agent-provisioned-worktrees.md:40`), and fork#175's live measurement found **0 of 8** orchestrator-shaped panes carrying any distinct owner identity — all 8 rooted at the same root checkout.
- **Model B — the `dispatch` command** (`src/dispatch.rs:263`, PRD #220): identical pattern — `clone_dir = ctx.working_dir.clone()`, worktrees are siblings of the user's own checkout.
- **Model C — scheduled issue-dispatch** (`src/issue_dispatch_run.rs:352`, PRD #120): **already a one-clone-per-task-name model.** `provision_repo` (`:1038`) clones fresh under `workspace.join(sanitize_clone_segment(task_name))` if that directory doesn't already exist, otherwise fetches and fast-forwards. Every issue *that same scheduled task* dispatches shares that one task-scoped clone via `.worktrees/issue-<n>` worktrees off it — but a **different** task name (even against the same upstream repo) gets a **different, fully separate clone**, automatically, today, with zero additional code.

So the real question is not "single clone vs. many" in the abstract — it is **whether Models A and B should adopt something closer to Model C's already-shipped, already-working per-task-scope isolation**, and PRD #236 already notes (in its own Out of Scope section) that #120 and #220 run two unreconciled clone strategies side by side.

### Correction 2 — the disk/build-cache tradeoff the issue names does not actually hold for build cache

Nothing sets `CARGO_TARGET_DIR` for ordinary builds — the only two setters anywhere in this codebase are `scripts/windows-cross-check.sh` and `tests/e2e_version_injection.rs`'s own child build, neither on the normal development path. CLAUDE.md rule 1 states the reason plainly: *"A worktree also keeps each branch's `target/` build state isolated, so switching work does not force a full rebuild"* — meaning **every worktree already gets its own independent `target/` today**, under the current single-shared-clone model, purely from cargo's ordinary per-directory default. Sharing one `.git` object store does not currently buy any orchestration a shared build cache — that per-worktree isolation is already treated as a *feature* (rule 1), not a cost.

The real, remaining cost of N clones instead of one is **git object-store duplication only** (pack files, one copy per clone) — not the build-cache loss the issue's own framing implied. That materially changes the tradeoff's weight, and no existing doc or PRD quantifies it (no `git clone --reference`, alternates, or any shared-object-store tooling exists anywhere in this codebase — a multi-clone direction would start from zero on that mitigation, not extend something already there).

### What reversing Model A/B's shared-clone default would actually cost in code

Every coordination primitive #325's predecessor PRDs built assumes single-clone containment:

- `owned_git_dir` (`src/worktree_reclaim.rs`, ~`:892-901` on `origin/main`) checks that a worktree's `.git` dir resolves under **one** `repo_dir`'s common dir — a multi-clone world needs this to enumerate and check against *every* clone the deck knows about, not just the caller's cwd.
- The attach-race lock (PR #331, `worktree_attach_lock_path`/`git_common_dir`, `src/issue_dispatch_run.rs:1491-1621`) is anchored under the shared clone's common `.git` dir — genuinely per-clone today; a multi-clone model would need either N locks or a different anchor.
- `WorktreeRegistry` (`src/issue_dispatch_run.rs:99-115`) is daemon-wide but its `WorktreeEntry` already carries a per-entry `clone_dir` — this part costs nothing extra for a multi-clone world.

## Decisions

| Question | Direction | Why |
|---|---|---|
| Should Models A/B (TUI orchestration, `dispatch`) move toward Model C's per-task/per-orchestration clone isolation? | **Yes, but scoped to concurrent-orchestration collision risk specifically** — not a wholesale rewrite of how the deck's own root checkout is used for the user's own interactive work. | Model C already proves the pattern works and ships today; the disk-cost objection that blocked considering this is weaker than assumed (build cache is unaffected either way). |
| Does this touch PRD fork#166's "intended" framing? | **Yes — needs an explicit amendment**, not a silent reversal. | fork#166 stated its choice deliberately; undoing part of it deserves the same deliberateness. |
| Object-store duplication cost — mitigate now, or accept it? | **Accept it initially**, revisit `git clone --reference`/alternates only if the disk cost is measured and found to matter. | No existing tooling to build on; premature optimization here risks a second unproven architecture change stacked on the first. |

### M1 correction — the trigger, corrected same day

M1 was first approved as "concurrent/automated orchestration provisioning (scheduled dispatch, delegate-provisioned orchestrations) gets its own clone; a human's own interactive TUI session is unaffected." Implementation-prep investigation (before any code was written) found this doesn't hold:

- **The actual #325 incident was N concurrent orchestrations, all spawned the human/TUI-form way** — one human, one deck, 3+ orchestration tabs open at once via `Action::SpawnPane` (`ui.rs:8767`/`:11155`, reachable only from a keypress or click on the New Pane form). "A human's own interactive session" is not a category the incident falls outside of — it *is* the incident, repeated.
- **"Delegate-provisioned orchestrations" is not a real code path.** PRD fork#175 (the PRD that would create it) is 0% implemented — a superseded design doc with no code trace anywhere (`Delegate`'s wire signal carries no path/branch/SHA field at all).
- **No code anywhere counts "how many orchestrations are already active."** There was no existing signal to gate on; the approved trigger required inventing one.

**Corrected trigger, replacing the row above:** the **Nth-concurrent-orchestration gate**. `live_orchestration_cwds_and_titles()` (`ui.rs:1011`) already computes, on every `Ctrl+n` form-open, whether the target cwd hosts a live orchestration — today purely a cosmetic warning banner via `live_orchestration_in_same_cwd()` (`ui.rs:957`), never a correctness gate. Repurposed: the **1st** orchestration against a root checkout shares it, exactly as today (no behavior change for the common case); the **2nd and later concurrent** orchestration against the *same* root checkout gets its own isolated clone. This matches the incident's actual shape — a 3rd orchestration colliding with two already-live ones — rather than a human/automated split the incident never had. Model B (`dispatch` CLI) has no equivalent live-orchestration query today and needs one added (Design step 4 below).

## Design

1. Extend Model C's `provision_repo` pattern (`src/issue_dispatch_run.rs:1094`, clone-if-absent + fetch/ff-pull otherwise, keyed on a scope identifier) to be reusable by Models A/B, keyed on the orchestration's own name/identity rather than an issue-dispatch task name.
2. `owned_git_dir` and the attach-race lock need to become clone-aware rather than single-repo-aware — likely by iterating a small registry of known clone roots rather than assuming exactly one. **Corrected 2026-08-20, M4b scoping investigation: this concern did not materialize in the shipped architecture and needs no code change.** Every real call site of `owned_git_dir` (`worktree_reclaim.rs:289/1040/1160`, `issue_claim.rs:175`) and `worktree_attach_lock_path`/`worktree_attach_lock_path_from_common_dir` (`issue_dispatch_run.rs:1383/2885`) already receives the single, correct `repo_dir`/`clone_dir` for the one specific worktree or clone it's checking — never a case needing to check one target against multiple candidate roots. M4a's isolated-clone discovery (`discover_isolated_clones`) is not built on `owned_git_dir` at all; it's a fully separate, parallel enumeration with its own `candidate_has_attach_lock` check. The "iterate a registry" framing assumed a shared-registry architecture that the actual M3/M4a implementation never adopted — each caller already knows exactly which repo it's asking about. Nothing here needs M4b work.
3. `WorktreeRegistry`'s existing per-entry `clone_dir` already supports this without a schema change (Correction 1 above) — the registry itself is not blocking.
4. **The gate itself**: at `Action::SpawnPane`'s worktree-provisioning point (`ui.rs:10214`), consult `live_orchestration_in_same_cwd`'s existing daemon query — no longer only for the cosmetic warning, but to decide provisioning: if the target cwd already hosts a live orchestration, provision via the (new, Model-A-reusable) `provision_repo`-style path instead of `create_worktree_sync` against the shared checkout. **The existing query fails open on a down daemon** (no warning, proceeds normally) — for a *gate*, not a hint, failing open means the exact race #325 originally reported (a down/wedged daemon hides a live sibling) reappears. This needs its own decision at M3 time: fail open (accept the residual race, same risk profile as everything else on a down daemon) or fail closed (refuse to spawn until the daemon answers) — flagged here so M3 doesn't inherit the hint path's fail-open behavior silently.
5. Model B (`dispatch.rs:263`) needs the equivalent live-orchestration query added — it has none today, unlike Model A which already computes one for the warning banner.

## Milestones

- [x] M1 — Maintainer decision recorded — **approved 2026-08-18, corrected same day**: the Nth-concurrent-orchestration gate (see "M1 correction" above), not the originally-approved human/automated split.
- [x] M2 — PRD fork#166 amended for the original framing (PR #478); the follow-up amendment this bullet used to flag as outstanding landed as part of M6 (this run): fork#166's note now describes the actual shipped Nth-concurrent-orchestration trigger and links `docs/develop/shared-clone-architecture.md`, replacing the stale description of the superseded human/automated split.
- [x] M3 — The Nth-concurrent-orchestration gate: `provision_repo`'s clone-if-absent pattern generalized and reused by Model A at `Action::SpawnPane`'s provisioning point, gated on `live_orchestration_in_same_cwd`'s existing query (repurposed from cosmetic warning to correctness gate); the fail-open-vs-fail-closed question (Design step 4) resolved explicitly, not inherited silently. Model A shipped in PR #481. Model B (`dispatch.rs`) got the equivalent query added in PR #504 (issue #490), plus the isolated-clone-specific rollback, tab-close cleanup, and `origin_warning` surfacing that path needed and Model A didn't (Model A never calls `record_worktree`). **Gap found and closed afterward: issue #489/PR #506** — the gate as shipped never fired on the default blank-slug spawn path at all; the fix added `SiblingScope::ExactCwdOnly` (blank-slug: refuse on exact-cwd collision only) alongside the pre-existing `AnySharedCommonDir` (typed-slug: isolate on any shared-common-dir collision), pinned by `orchestration/worktree/017`.
- [x] M4 — Originally scoped as `owned_git_dir`/attach-race lock clone-awareness; **that concern turned out to need no code change (see Design item 2's 2026-08-20 correction and the struck sub-bullet below)**. M4's real content, named by M3's fix round without this milestone previously naming it: an enumeration/discovery surface for permanently-retained isolated clones (`RemovalPolicy::IsolatedClone` never auto-removes one — `worktree list`/`reclaim` only walk `git worktree list` from the root checkout, which structurally cannot see a separate clone repository) and, if a safe signal for it is found, actually reclaiming one — not merely a clone-aware `owned_git_dir`/attach-race lock. `src/issue_dispatch_run.rs`'s and `src/event.rs`'s doc comments on `RemovalPolicy::IsolatedClone` / `KeptReason::IsolatedClone` both cite "PRD fork#325 M4" for this; this bullet is what makes that citation resolve to real scope instead of an empty milestone.
  - [x] **M4a (landed, PR #510)** — the discovery half only: `worktree list`/`worktree reclaim` now enumerate deck-owned isolated clones sitting as siblings of the root checkout, reported distinctly (`kind: "isolated_clone"`) and never assigned an automatic-removal verdict. Deliberately conservative: it answers "can the deck see these at all" and stops there.
  - [x] **`owned_git_dir`/attach-race lock "clone-aware" concern — resolved 2026-08-20, no code needed.** See the correction on Design item 2 above: every real call site already receives the single correct repo/clone root for what it's checking; the "iterate a registry" framing never matched the shipped architecture. Struck from M4b's scope.
  - [x] **M4b (landed, PR #515)** — closed all three residuals, plus a self-inflicted blocker along the way, worth recording honestly rather than glossing over: the first fix attempt moved ownership evidence into a dedicated provenance artifact written *inside each candidate's own `.git`* — which closed the two originally-scoped forgery-cost residuals (P1: no more inheriting a stale linked-worktree lock; C1: the lock-before-exists-check ordering no longer leaks attribution) and F2 (`--mine` from inside a clone), but reopened auditor A1/B1's ORIGINAL forgery at the cost of one `touch`, since the candidate's own `.git` is exactly the location a same-uid attacker planting a sibling directory already controls. Both reviewer and auditor caught this independently before merge, each with a working reproduction (test `worktree/reclaim/061` pins it). The shipped fix relocates the artifact a second time, entirely outside every candidate, into `crate::platform::paths::state_dir()`, keyed by `fnv1a64` of the candidate's canonical clone path, in an owner-only directory — the same trust boundary `owned_git_dir` has always relied on for linked worktrees, this time genuinely extended rather than shortcut. B4's remaining coverage (symlink filter, `core.fsmonitor` isolation) was closed in M4a's own final round, not deferred here as originally scoped.
  - [x] **M4c (shipped, maintainer-decided rule) — an isolated clone can become automatically reclaimable under a five-condition safety rule, redesigned around the PR's own `headRefOid`.** Shipped in three rounds. **Round 1** compared only the clone's `MERGED`-PR commit-SHA against the merge commit's own SHA. An audit of that version found 3 blocker-severity gaps: (A2) eligibility never consulted `has_attach_lock` at all, so a forged/unowned sibling directory — no deck involvement required — could satisfy it; (A1) eligibility never consulted cleanliness, so a dirty tree could still report reclaim-eligible even though `remove_dir_all` has no equivalent of `git worktree remove`'s own refusal against a dirty tree; (A8) on a squash-merge repo a genuine clone's own commit SHA essentially never equals the PR's `mergeCommit.oid` (GitHub's squash merge creates a brand-new commit object), so the rule was inert for exactly the real clones this milestone exists to reclaim, while a planted candidate could satisfy it trivially with one `git checkout`. **Round 2** required the AND of all five: `has_attach_lock`; `Cleanliness::Clean`; exactly one local branch (the resolved current one, no others); an empty `git stash list`; and the clone's HEAD **tree** SHA equal to the merged PR's merge-commit **tree** SHA (`gh api graphql`'s `mergeCommit.tree.oid`, since `gh pr list --json` cannot reach a sub-selection into `mergeCommit` at all) — content equality rather than commit-SHA equality, since trees agree once nothing local remains even under a squash merge. **Round 3 (the shipped rule, PR #526, reviewer B2)** found round 2's fifth condition itself inert: live measurement against this very repo showed a genuine deck-provisioned clone's own HEAD is (almost) never equal to `mergeCommit.oid` under any GitHub merge strategy (PR #481 head `7339edd5f440` vs merge `1ceb919349ef`; PR #477 head `11d6327f2421` vs merge `5742ad1f93dd`), so round 2's tree comparison could (almost) never fire in production. Round 3 drops the `mergeCommit`/tree machinery (and its second `gh api graphql` round trip) entirely and replaces the fifth condition with plain HEAD-**commit**-SHA equality against the merged PR's own `headRefOid` — the PR branch's own tip commit, exposed directly as a flat `gh pr list --json headRefOid` field. The other four conditions (`has_attach_lock`, `Cleanliness::Clean`, single local branch, empty stash) are unchanged from round 2. Round 3 also closed two further findings from the same review: (M1) `remove_isolated_clone_dir` now re-verifies the full eligibility signal set (cleanliness, single branch, empty stash, HEAD SHA vs. `headRefOid`) immediately before the actual `remove_dir_all`, fresh from disk rather than trusting the examination pass, closing the TOCTOU window a large reclaim batch can open; (M2) the rule's residual liveness gap — these five conditions prove provenance and content safety, never that the clone isn't currently in active use — is now an explicit, documented residual on `isolated_clone_report`'s own doc comment rather than an implicit one, since `worktree reclaim` is a plain CLI subprocess with no reachable connection to the daemon's in-process liveness tracking. `None`/unresolvable anywhere in the chain fails closed to "not eligible." See `isolated_clone_report`'s doc comment in `src/worktree_reclaim.rs` and `docs/develop/shared-clone-architecture.md` for the full shape. Carries auditor A1's finding (PR #510): `owned`/`owner_kind` for an isolated-clone row was weaker evidence than a linked row's — since M4b this is a documented, empirically-verified honest limit (same-uid, no containment analogue possible for a separate repo) rather than a silent gap; the tightened rule's `has_attach_lock` gate inherits that limit and accounts for it explicitly (round 1 did not). Auditor C2 (cross-platform attach-lock-artifact write asymmetry) is **retired** — M4b's fix removed the dead Windows artifact-touch entirely (reviewer R3), so the asymmetry it named no longer exists. New from M4b's final round, both Low severity and tracked as their own issues rather than folded in here since neither is M4c-specific: [E1](https://github.com/prageethw/dot-agent-deck/issues/516) (`state_dir()` has no emptiness guard on `DOT_AGENT_DECK_STATE_DIR`, pre-existing but newly security-relevant since M4b), [E2](https://github.com/prageethw/dot-agent-deck/issues/516) (unit tests don't sandbox `DOT_AGENT_DECK_STATE_DIR`, writing real artifacts into the developer's/CI runner's actual state directory).
- [x] M5 — Tests: reproduce #325's actual incident shape (a 3rd orchestration racing two already-live ones on the same root checkout) under the new gate and show it can no longer collide — not a synthetic two-orchestration case that doesn't match what actually happened. Shipped: PR #517 (`orchestration/worktree/016`), merged `b014a7ba`. (`worktree/reclaim/049`–`061` are M4a/M4b's own tests, shipped in PR #510/#515 respectively — not this PR's.)
- [x] M6 — `docs/develop/` updated to describe the resulting model (which callers share a clone, which don't, and why, and what fail-open/closed choice M3 made), so a future contributor doesn't rediscover Model A/B/C's divergence the hard way, the way this PRD's own investigation had to. Shipped: `docs/develop/shared-clone-architecture.md` (new), plus the fork#166 note amendment folded into M2 above.

## Test plan

L2 required — this is inherently about real concurrent git/filesystem behavior, not renderable in an L1 widget test. The reproduction target is #325's own original incident shape: two orchestrations, one attempting a worktree removal the other still has live, under whichever clone-isolation boundary M1 settles on — proving the boundary actually prevents the collision, not merely that it changes where clones live.

## Out of scope

- Object-store sharing mitigation (`git clone --reference`/alternates) — deferred pending a measured cost, per the Decisions table.
- **The 1st orchestration against any given root checkout** — unaffected either way; it always shares the checkout as today, whether or not a 2nd one later arrives.
- Finishing PRD fork#175 (delegate provisions the worktree) — unrelated; this PRD's gate does not depend on it.
- Re-litigating directions 1-3 of the original #325 issue — those are shipped (PR #331, #420, #458, #471).
- **Model B's branch-reuse inconsistency between its two arms** (fix round 2, reviewer P2-7) — the shared arm refuses to reattach an existing branch (`WorktreeCreation::BranchExists`, to protect committed work); the isolated-clone arm attaches to it silently (inherited from Model A, which does this on both of its own arms and is internally consistent). Which of Model B's two arms runs on a given `dispatch <name>` depends on the timing-sensitive `has_live_sibling` daemon query, so the same command can refuse on one invocation and silently attach to a prior branch's commits on the next, with nothing in the output distinguishing the two outcomes. Known, deliberately deferred — this is a real design question (should the shared arm's refusal move to match the isolated arm's attach-not-refuse, or vice versa?), not something to resolve as a side effect of the M3 fix round.
