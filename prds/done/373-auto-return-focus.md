# PRD #373: Auto-return focus to the orchestrator pane (Orchestration tabs only)

**Status**: All milestones complete — M1, M2, M3 and M4 ship together on this branch. **M2 and M3 were subsequently REMOVED IN FULL by [#393](https://github.com/vfarcic/dot-agent-deck/issues/393) (`prds/393-command-mode-lock-and-lock-governed-focus.md`) and no longer exist in the product.** M1 stays as shipped, live behaviour — with one change: #393 gates the whole surviving focus chain on the deck-global command-entry lock, so M1's all-clear move fires only while the deck is locked. Both removals are annotated in place below; the original reasoning is left as written. M3 was briefly carved out into [#383](https://github.com/vfarcic/dot-agent-deck/issues/383) (`prds/383-blocked-keystroke-reset.md`) while #374 was still in flight, then folded back in here once #374 landed (`936a603`); #383 is delivered by this PR rather than separately. The PTY-attached L2 test (CLAUDE.md rule 4) landed as `orchestration/focus/001` — its first real run caught a genuine M2 timer bug (see M2's post-e2e-gate fix note) that no unit test had exposed; fixed by gating M2 on `command_entry_locked`. Full `cargo test-e2e` run and PR opened.
**Priority**: Medium
**Created**: 2026-08-04
**GitHub Issue**: [#373](https://github.com/vfarcic/dot-agent-deck/issues/373) (closed upstream as not-planned; this fork continues the work independently)
**Related**: Split from #361, alongside #371/#372/#374 (siblings). Structurally depends on [#374](https://github.com/vfarcic/dot-agent-deck/issues/374) (command-entry lock) for its combined interaction test — see Technical Approach and Open Questions. `src/tab.rs` (`Tab::Orchestration`, `role_pane_ids`, `focused_role_pane_id`, `start_role_index`, `auto_focus_waiting_pane`, `auto_focus_all_clear` — fork-only), `src/ui.rs` (`UiState::last_pane_keystroke_at`, `Action::ForwardToPane`, per-frame call site around `src/ui.rs:10304-10317` — line drifted from the M1-time `9750-9758` reference below)

## Problem Statement

Confirmed with the user: **scoped to Orchestration tabs only**, not Dashboard or Mode tabs. Two related behaviors:

1. When every role pane in the active Orchestration tab that was `WaitingForInput` has resolved (none remain waiting), focus should move to the orchestrator pane specifically.
2. If the human manually moves focus to a different pane in that tab, and 30 seconds pass with no further human activity, focus automatically snaps back to the orchestrator pane.

### Relationship to existing auto-focus behavior

`src/tab.rs`'s `Tab::Orchestration` already carries `role_pane_ids: Vec<String>`, `focused_role_pane_id: Option<String>`, and `start_role_index: usize` — "the orchestrator" is `role_pane_ids[start_role_index]` (`src/tab.rs:96-117`); `focused_role_pane_id == None` already means "default to the start/orchestrator role pane on switch-in" (`src/tab.rs:104-106`), an existing precedent for orchestrator-targeted focus this PRD extends to a *new trigger* rather than only tab switch-in.

The fork-only (not upstream) `TabManager::auto_focus_waiting_pane` (`src/tab.rs:409-432`, wired once per render frame at `src/ui.rs:9756-9758`) is a **related but distinct** behavior: it fires on the *waiting* condition (steers focus to the lowest-`role_pane_ids`-order pane that currently *is* `WaitingForInput`), continuously re-evaluated every frame from the live `pane_status_for_tabs` join (`build_pane_status(&snapshot)`, same join PRD #333's tab-label coloring already reads). This PRD's first behavior fires on the opposite, *all-clear* condition and always targets the orchestrator role specifically, not "whichever pane needs attention." **The two must coexist on this fork**: `auto_focus_waiting_pane` continues to steer focus toward a newly-waiting pane while one exists; this PRD's all-clear behavior takes over the instant the last one resolves. They are not in tension as event-driven states — a pane cannot be simultaneously "some pane is waiting" and "no pane is waiting" — but both need to keep being called from the same per-frame site (`src/ui.rs:9750-9758`), with the all-clear check naturally gated to run only when `auto_focus_waiting_pane` finds nothing to steer toward.

Because both behaviors evaluate every frame from a live status snapshot, the all-clear focus move must be **edge-triggered** (fires once, on the frame where "some pane was waiting" transitions to "none are"), not level-triggered (which would fight the second behavior below — if focus were pinned back to the orchestrator on every frame merely because nothing is currently waiting, the human could never manually look at another pane at all, defeating the point of the 30-second grace window). This requires a small piece of new per-tab state — e.g. `had_waiting_pane: bool`, refreshed each frame alongside the existing waiting-pane check — that the current `auto_focus_waiting_pane` doesn't need (it's naturally edge-driven already: it only ever moves focus toward a *specific new* waiting pane and no-ops once already there, per its own doc comment).

## Solution Overview — the 30-second inactivity timer

**Activity definition, resolved rather than left vague**: the codebase already tracks exactly the right signal for this. `UiState::last_pane_keystroke_at: Option<std::time::Instant>` (`src/ui.rs:1725`) is updated at the single choke point where a keystroke is actually forwarded into a focused pane's PTY — `Action::ForwardToPane`'s dispatch arm (`src/ui.rs:8243-8267`) and the paste-handling path (`src/ui.rs:11200-11225`) — and nowhere else (it is not touched by agent/hook activity, only by human keystrokes actually reaching a pane). Since only one pane can hold focus at a time and only the active tab's focused pane can receive forwarded keystrokes, this single, already-global timestamp is precisely "the human is actively typing into the currently-focused pane in the active tab." Proposed definition: **"activity" = a forwarded keystroke into the focused pane's PTY**, i.e. an update to `ui.last_pane_keystroke_at`. The 30-second timer starts when focus lands on a non-orchestrator pane in an Orchestration tab and resets on every such update; if it elapses with no update, focus snaps back to the orchestrator pane (`role_pane_ids[start_role_index]`).

**Decision**: this activity definition interacts with #374's command-entry lock. If #374's lock is engaged (its default state) while focused on a non-orchestrator pane, keystrokes typed there are — by #374's own design — dropped *before* they reach `Action::ForwardToPane`, so `last_pane_keystroke_at` never updates no matter how much the human fiddles with the locked pane. Resolved by the user: a blocked/attempted keystroke to a locked pane **does** count as activity — it resets the 30-second inactivity timer, since the human is still clearly engaged with that pane even though nothing was forwarded to the PTY. This means the reset point cannot be `last_pane_keystroke_at` alone (that timestamp only updates on an actually-forwarded keystroke); the gate inside #374's PTY-forward fallback needs to also record an attempt — e.g. update a `last_pane_activity_at`-style timestamp (or reuse/extend `last_pane_keystroke_at` itself to be set on both the forwarded and the blocked-drop path) before returning `Action::Continue` for the dropped key.

## Scope

**In Scope**: `TabManager` gains the all-clear focus-move (new method alongside `auto_focus_waiting_pane`, called from the same per-frame site, `src/ui.rs:9750-9758`) plus the edge-trigger state it needs; a 30-second inactivity timer keyed off pane-forwarded and pane-blocked keystroke activity (see Decision above), scoped to the active Orchestration tab, that refocuses the orchestrator pane; unit tests for the edge-trigger (no repeated snap-back once already on the orchestrator with nothing waiting) and the timer (elapses → snaps back; resets on both a forwarded keystroke and a blocked keystroke on a locked pane).

**Out of Scope**: Dashboard and Mode tabs (per user confirmation); changing `auto_focus_waiting_pane` itself.

## Technical Approach

The edge-trigger and timer mechanics are described in Solution Overview above. One dependency worth stating precisely for sequencing this branch's work:

**Dependency on #374 (command-entry lock)**: the "blocked keystroke counts as activity" half of this PRD's Decision only has an observable effect once #374's lock exists — before that lands, every keystroke on a focused non-orchestrator pane is always forwarded (there is nothing to block), so the timer's reset behavior collapses to the single `last_pane_keystroke_at`-only case. This PRD's own scope (the edge-trigger, the timer, the orchestrator-targeted refocus) does not require #374 to be implemented first and can land independently. But the **combined interaction test** — verifying a blocked keystroke on a locked pane resets the timer — is structurally blocked on #374's lock existing, since there is no locked pane to attempt a blocked keystroke against without it. That test should be **deferred until #374 lands**, tracked as a follow-up on this PRD rather than blocking M1/M2 below.

## Success Criteria

- The moment the last `WaitingForInput` role pane in the active Orchestration tab clears, focus moves to that tab's orchestrator pane exactly once (not repeatedly every frame).
- Manually focusing a non-orchestrator pane in an Orchestration tab, then leaving it untouched (per the resolved activity definition) for 30 seconds, snaps focus back to the orchestrator pane.
- No effect on Dashboard or Mode tabs, and no fighting between this behavior and `auto_focus_waiting_pane` (fork-only) — verified by a test exercising both in sequence (a pane starts waiting → gets steered to → resolves → all-clear steers back to orchestrator).

## Milestones

- [x] **M1 — All-clear focus move.** New edge-triggered `TabManager` method alongside `auto_focus_waiting_pane`, wired at the same per-frame site (`src/ui.rs:9750-9758`); unit tests for the edge-trigger (fires once, not every frame) and coexistence with `auto_focus_waiting_pane`. Landed as `TabManager::auto_focus_all_clear` (`src/tab.rs`) + `Tab::Orchestration::had_waiting_pane` edge state, wired at `src/ui.rs` (line drifted to ~10310); test `tabs/orchestration/012`. **Review fix:** the waiting-history observation was folded into `auto_focus_all_clear`, which the chain only reaches when `auto_focus_waiting_pane` returned `None` — so a waiting episode observed in a single frame (the frame the steering branch consumes) was never recorded and its all-clear move never fired. The observation now lives in `TabManager::observe_waiting_panes`, called once per frame **unconditionally** ahead of the chain, latching the `true` → `false` edge into `Tab::Orchestration::all_clear_pending` for `auto_focus_all_clear` to consume; the focus move stays edge-triggered. Test `tabs/orchestration/020`.
> **REMOVED BY #393 (M4a) — M2 and M3 below no longer exist.** Both were deleted outright, as a pure subtraction: `TabManager::auto_focus_after_inactivity` and its `INACTIVITY_TIMEOUT`; `Tab::Orchestration::last_role_pane_activity_at` and **all six** stamp sites; `TabManager::inactivity_timeout_from_env` and the `DOT_AGENT_DECK_INACTIVITY_TIMEOUT_SECS` test seam M5 introduced; the blocked-keystroke stamp at the lock's drop site; and the third branch of the per-frame focus chain. Tests `tabs/orchestration/013`–`019` and `021`–`023` went with it, as did the PTY test `orchestration/focus/001` (replaced by `orchestration/focus/002`, which covers the successor behaviour).
>
> **Why.** Two reasons, and the first is sufficient on its own. **(1)** The user's direction: focus should simply stay where it is put. **(2)** The mechanism was never fully sound, on this PRD's own record — its wall-clock "time since last stamp" check is racy against render-loop stalls (measured at `elapsed=4.86s` against a 2s threshold, 100% reproducible, and caught only by the PTY test), and the shipped mitigation of arming it only while locked narrowed that race rather than closing it. It also required six stamp sites kept in sync, and **four separate review findings on this PRD were each a missed or over-broad stamp at one of them** — a maintenance surface with no remaining justification once the behaviour is gone.
>
> **What replaced it.** Nothing, deliberately. #393 decision 5: while **locked**, focus steers to a `WaitingForInput` role pane in ascending `role_pane_ids` order and returns to the orchestrator on the all-clear edge (M1, still live). While **unlocked**, no auto-focus branch fires at all — the whole chain, including `observe_waiting_panes`, is skipped, so focus stays exactly where the human put it. There is no timer in either state. One piece of genuinely new state logic came with that gating: the edge latch is cleared on the locked→unlocked transition, so a waiting episode that *straddles* the transition cannot be misread as a fresh all-clear on re-lock (`tabs/orchestration/026`).
>
> **The mitigation this PRD introduced turned out to be the seed of the replacement.** Gating M2 on `command_entry_locked` established that the lock governs whether the focus machinery runs at all; #393 generalised that from one branch to the whole chain.

- [x] **M2 — 30-second inactivity snap-back.** ~~Live behaviour.~~ **Deleted by #393 — see the note above.** Timer scoped to the active Orchestration tab, reset by a forwarded keystroke (per the Decision above); unit test for elapse → snap-back and reset-on-activity. Landed as `TabManager::auto_focus_after_inactivity` (`src/tab.rs`) reading `Tab::Orchestration::last_role_pane_activity_at`, stamped at every site that reaches a role pane's PTY or moves focus, and wired as the third branch of the per-frame chain in `src/ui.rs`. The timestamp lives **per tab** rather than on `UiState` — the scoping fix in `441f043`, without which unrelated Dashboard/Mode activity could suppress an otherwise-due Orchestration-tab snap-back. Tests `tabs/orchestration/013`–`018`: `013` the resolver's timing rules, `014` the render-loop wiring plus the per-tab scoping fix, `015` the `dispatch_normal_mode_key` cycling stamp site, and `016`/`017`/`018` pinning three confirmed review findings fixed in `23dc7a6` (the timer fighting `auto_focus_waiting_pane`, the `send_config_gen_prompt` stamp gap, and the unbound-key stamp). A later review pass added three more: `021` proves the snap-back cannot yank focus off a still-`WaitingForInput` role even on the first frame of the episode (driven through the real chain, the safety half of M1's `observe_waiting_panes` fix); `022` pins that switching BACK to an Orchestration tab stamps its clock, so the restored role pane gets the full grace interval instead of being snapped away on the first frame back; `023` pins that a click on the ALREADY-selected card no longer stamps, closing the same over-broad accounting `018` fixed for unbound keys. The all-clear branch of the per-frame chain also gained the pending-input `poll(0ms)` guard the snap-back branch already had — see the residual-race entry under Risks.
  **Post-e2e-gate fix — gate M2 on `command_entry_locked`:** the first real run of `orchestration/focus/001` (M5, see below) caught a genuine bug the unit-level tests couldn't see: `auto_focus_after_inactivity`'s wall-clock "time since last stamp" check is racy against render-loop stalls — a frame that takes even a few hundred ms to drain a backlog of queued keystrokes makes the last stamp look stale even though the user never stopped typing, so continuous activity on an unlocked worker pane could still get snapped away. Traced with temporary instrumentation to a confirmed `elapsed=4.86s` gap against a 2s test timeout despite steady keystroke delivery — 100% reproducible, present since before this branch's review-fix commit, not a flake. Fix (per user direction): since #374 locks command entry to the orchestrator by default, genuinely continuous typing on a role pane is only possible after a deliberate Ctrl+e unlock — so the M2 branch now only arms `Some(last_activity_at)` while `Tab::Orchestration::command_entry_locked` is `true`. While unlocked, the branch is skipped outright: no timer runs, so there is no stale timestamp to misread. Re-locking (or leaving the tab) resumes normal inactivity tracking. This closes the race for the unlocked case entirely rather than trying to make the wall-clock check itself more robust.
- [x] **M3 — Blocked-keystroke reset — folded back in and shipped here.** ~~Live behaviour.~~ **Deleted by #393 — see the note above M2. There is no timer left to reset, so #383's subject is gone with it.** Briefly carved out into [#383](https://github.com/vfarcic/dot-agent-deck/issues/383) (`prds/383-blocked-keystroke-reset.md`) while #374 was still in flight; once #374's lock landed (`936a603`) the user decided M3 ships in this PR rather than separately, so #383 is delivered by this branch. The lock's drop site in `handle_key_event`'s `UiMode::PaneInput` arm (where a gated `Action::ForwardToPane` becomes `Action::Continue`) now stamps `Tab::Orchestration::last_role_pane_activity_at` before returning, so a keystroke blocked by the lock defers the snap-back exactly as a forwarded one does. Combined-interaction test `tabs/orchestration/019` drives the REAL `handle_key_event` against a locked tab and asserts both the drop and the deferred snap-back.
- [x] **M4 — Docs and changelog.** **(#393: the `docs/orchestration.md` section this milestone added was rewritten and renamed to "Focus follows the lock" — two of its three paragraphs described the deleted timer. The released `373.feature.md` fragment is history and stays as written; `changelog.d/393.feature.md` records the removal for users who relied on the snap-back.)** Changelog fragment at `changelog.d/373.feature.md` (feature; covers the all-clear focus move, the 30-second snap-back, and the blocked-keystroke reset). `docs/orchestration.md` gained a "Focus returns to the orchestrator on its own" section describing all three. No `docs/keyboard-shortcuts.md` change was needed — this PRD introduces no new keybinding, only timer/focus behavior.
- [x] **M5 — PTY-attached L2 coverage (CLAUDE.md rule 4).** **(#393: `orchestration/focus/001` was deleted along with the behaviour it covered, and with it the `DOT_AGENT_DECK_INACTIVITY_TIMEOUT_SECS` seam described below. `orchestration/focus/002` replaces it in the rule 4 headline slot, covering the locked/unlocked focus contract that actually ships.)** `tests/e2e_orchestration_focus.rs`'s `orchestration/focus/001` drives the real binary over a PTY: Part 1 focuses a non-orchestrator role while the tab is still LOCKED (PRD #374's default), waits out the interval with zero input, and asserts on the RENDERED GRID that a subsequent keystroke lands in the orchestrator's own expanded pane box; Part 2 (the control) deliberately unlocks (Ctrl+e), re-focuses the worker role, and keeps it alive with continuous keystrokes, asserting no snap-back. This is the test that caught the M2 lock-gating bug above — it was RED end-to-end (in two different ways: the real timer race, then a test-only sentinel-line-wrap false negative once the real fix landed) until both were fixed, confirming the M2 "landed" claim in an earlier version of this doc had never actually been backed by a passing e2e run. Because a spawned process cannot be handed a fake `Instant`, the sole per-frame call site now reads its interval through `TabManager::inactivity_timeout_from_env()` — a **test seam** honoring `DOT_AGENT_DECK_INACTIVITY_TIMEOUT_SECS` (integer seconds; absent/unparseable/`0` falls back to the 30-second `INACTIVITY_TIMEOUT`), mirroring the existing `DOT_AGENT_DECK_IDLE_SHUTDOWN_SECS` precedent. Deliberately not a user-facing config option.

## Risks

- **Cross-item coupling with #374.** ~~Risk~~ — resolved by M3. This item's inactivity timer and #374's lock share the same keystroke-forwarding choke point (`Action::ForwardToPane`); the Decision above (a blocked keystroke on a locked pane still resets the timer) meant the gate inside #374's PTY-forward fallback had to record that attempt before dropping the key, not just short-circuit. It now does: the drop site stamps `last_role_pane_activity_at`, and `tabs/orchestration/019` pins it at the seam.
- **~~Known limitation: blocked keystrokes don't reset the timer.~~** Closed by M3, which ships in this same PR. It would have held only if M1+M2 had shipped ahead of the blocked-keystroke reset: a keystroke blocked by #374's lock would not reset the inactivity clock, so a user typing at a locked pane would look idle and could be snapped to the always-unlocked orchestrator after 30 seconds, with their next keystrokes landing there. Since M1+M2+M3 ship together, the limitation never reaches a release.
- **Known limitation (accepted, deferred): a residual input/focus race remains — narrowed to the LOCKED case only.** Both auto-focus branches that move focus AWAY from where the user is looking — the M1 all-clear move and the M2 snap-back — skip themselves for a frame when `crossterm::event::poll(0ms)` shows input is already queued, so a keystroke sitting in the OS buffer is dispatched to the pane it was aimed at before focus moves. That closes the wide window, but not all of it: a key that arrives *after* that poll and *before* `pane.focus_pane(&new_id)` completes is read on the next drain, after focus has already moved, and is forwarded to the **orchestrator's** PTY — which #374's command-entry lock deliberately does not gate. The real fix is to bind each captured input event to the pane that was focused when it arrived (a focus-generation handoff) so a stale-focus event can be routed or dropped. That re-architects the input loop and is deliberately **deferred to its own PRD** rather than rushed into this branch. **Update:** the M2 lock-gating fix above closes this race for the *unlocked* case entirely — M2 never arms while unlocked, so there is no timer decision to race against a keystroke in the first place. The residual sub-millisecond race still theoretically applies to the *locked* case's blocked-keystroke path (M3): a blocked keystroke still stamps the clock and could in principle race the same poll(0ms)/`focus_pane` window. That narrower residual is unverified either way — it wasn't specifically re-tested after the M2 fix — and is left under this same deferred-to-its-own-PRD umbrella rather than claimed closed.

**Update 2 — #393 narrowed the window further but did NOT close it, and says so explicitly.** Deleting M2 and M3 removes the branch that fired *unprompted*: there is no longer any timer decision to race a keystroke against, and the blocked-keystroke stamp that the paragraph above named as the remaining locked-case path is gone with it. What survives is M1's all-clear move, which fires only on a status edge — something the human can see coming — and only while the deck is locked, since #393 gates the whole chain on the lock. So the race is now reachable through exactly one branch instead of two, and only on an observable trigger.

It is still open. The mechanism is unchanged: a key that arrives *after* the `poll(0ms)` check and *before* `pane.focus_pane(&new_id)` completes is read on the next drain, after focus has already moved, and is forwarded to the **orchestrator's** PTY — which the command-entry lock deliberately does not gate. The real fix remains the focus-generation handoff (bind each captured input event to the pane focused when it arrived), which re-architects the input loop. **#393 lists this under Out of Scope and states in terms that it must not claim the race closed.** Deferred to its own PRD, still.
- **Experimental-flag candidate, decided against.** Per CLAUDE.md rule 9, this was raised as a candidate for gating behind `experimental` — it's a new, potentially surprising behavior (focus moves out from under the user without a keypress) scoped to Orchestration tabs. Resolved at `/prd-start` for this branch: ship visible by default, no flag. No `features::show_*` wrapper, no graduation follow-up needed.

## Open Questions

1. ~~Experimental flag gating~~ — Resolved at `/prd-start`: ships visible by default, not gated behind `experimental` (see Risks above).
2. ~~**M3 sequencing**~~ — Resolved: this PRD's M1/M2 landed independently of #374, and the blocked-keystroke reset was briefly split out into [#383](https://github.com/vfarcic/dot-agent-deck/issues/383). Once #374's lock landed (`936a603`) the user decided M3 should ship in this PR after all, so it was folded back in here (with its combined-interaction test `tabs/orchestration/019`) and #383 is delivered by this branch rather than separately.

Resolved (previously open, decided by the user before implementation started):

- **Does "activity" include blocked keystrokes on a locked pane?** Resolved: yes — a blocked/attempted keystroke to a locked pane counts as activity and resets the 30-second timer (see Solution Overview's Decision).
- **Experimental flag gating?** Resolved at `/prd-start`: no — ships visible by default.
