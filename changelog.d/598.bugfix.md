Corrects several code comments near the delegate `--subject` sanitization path that had drifted out of sync with the code they describe: two claimed a second sanitization pass happens on the delegated/expected side at the mismatch-comparison and warning-rendering sites, when in fact that side is sanitized once, at delegate time, and only the worker's echoed side is ever re-sanitized later; a third listed the wrong set of shell metacharacters one of its own test fixtures actually exercises. Extracts a shared `footer_subject_argument` helper so the `work_done_footer` round-trip test can no longer silently desync from production. Replaces a test that armed its fixture directly, without exercising the real delegate flow, with one that drives `handle_delegate` end to end and confirms the delegation-mismatch ledger's `expected` field stays free of a zero-width space and frame-breaking characters for a hostile delegated subject.

No-Test: the changes are either comment-only prose corrections with no observable behavior, or tests exercising internal invariants (the `delegate_mismatch_expected_side_is_canonical_for_a_frame_breaking_subject` integration test, the repointed `work_done_footer` round-trip test) — nothing here is TUI-observable behavior a `#[spec]` catalog test could pin.
