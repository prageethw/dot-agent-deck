Corrects several stale/inaccurate code comments left over from PR #612's review — a claim that a second `sanitize_subject_tag` re-application exists at the echo-comparison site when it does not (and directly contradicts another comment edited in the same commit), a backtick-strip justification naming only the weaker of its two sinks, and a comment overstating how far `--subject`'s and `--task-file`'s quoting idioms diverge. Extracts a shared `footer_subject_argument` helper so the `work_done_footer` round-trip test can no longer silently desync from production the way it did across bugs S11/H6/S13. Adds a unit test proving the `SubjectMismatch` ledger's `expected` side stays canonical for a frame-breaking subject, closing part of issue #598's deferred S15 gap without touching the async PTY test S15's full closure would require.

No-Test: the changes are either comment-only prose corrections with no observable behavior, or plain unit tests (`retire_delegation_commission_expected_side_is_canonical_for_a_frame_breaking_subject`, the repointed `work_done_footer` round-trip test) exercising internal Rust invariants — nothing here is TUI-observable behavior a `#[spec]` catalog test could pin.
