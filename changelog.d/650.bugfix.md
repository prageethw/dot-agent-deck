Fixes the agent-type badge (`Ctrl+m` / `m`) staying suppressed on a Codex session whose "Starting…" placeholder had already resolved via real activity, even though it carried a known model — the badge gate keyed off the raw `is_placeholder` flag (true for as long as `agent_type` stays untagged, which for Codex can be indefinite) instead of `is_empty_placeholder` (issue #549/PR #641's distinction between a genuinely-empty placeholder and one that has since done real work), the same gate the status label and border already use. A resolved Codex placeholder with a known model now shows it in the badge (e.g. `No agent (some-model-id) · ...`); a genuinely-empty, never-resolved placeholder still shows the badge off with the plain "No agent" status.
