Fixes a dashboard card getting stuck showing "Starting…" forever for an agent that never posts a `SessionStart` hook before its first turn completes (Codex's shape). The placeholder now clears the moment any real, non-synthetic status assertion arrives, even while the producer is still untagged, and the fix survives a detach/reconnect: a session that had already resolved before disconnecting no longer renders "No agent" again immediately after reconnect. A resolved placeholder's card is now internally consistent too — the body line, recent-prompt lines, and border all switch to the normal working presentation together with the status label, instead of the label alone updating while the rest of the card kept its empty-slot appearance.
