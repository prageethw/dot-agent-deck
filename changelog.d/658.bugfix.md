Adds `codex/live/002`, a real-agent e2e test proving Codex's model reporting (#652/#657) works against a genuine live `codex` process rather than only against byte-frozen capture data. It drives a real interactive Codex session through a minimal reply-only turn and asserts the daemon's captured `Thinking` event carries the actual active model read off that live process's own status bar. This closes a coverage gap the auditor flagged during #652's review: every existing model-carrier test (`codex/wrap/015`-`018`) pins one Codex TUI rendering as fixed capture bytes and could silently regress to a no-op with a fully green board if that rendering ever changes — "exactly how #652 happened" the first time. This is test-only; no production code changed.
