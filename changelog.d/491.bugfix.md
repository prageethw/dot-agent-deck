## `ensure_owner_only_dir` no longer follows a symlink planted at the target path

`ensure_owner_only_dir` (`src/platform/fsperm/unix.rs`) created and `chmod`'d the directory at the caller-supplied path without first checking whether that path was itself a symlink. A same-uid attacker who pre-planted a symlink at the target path could get their own directory silently tightened to `0o700` and treated as owner-only, regardless of what real permissions it started with — the caller believed it had secured its own directory when it had instead validated an attacker-controlled one.

The function now stats the target path with `symlink_metadata` (lstat semantics, which does not follow the final path component) before creating or chmod'ing anything, and refuses with an `io::Error` if a symlink is found there. This narrows the attack window rather than closing it — a symlink planted after the `lstat` check still produces the original outcome (issue #560) — and it is not limited to first-time creation: a pre-existing, legitimately-symlinked state directory (e.g. an intentional user setup, not an attack) now also fails this check, with daemon spawn/attach/logging hard-failing as a result.

No-Test: pinned by a plain `#[test]` in `src/platform/fsperm/unix.rs`'s new `mod tests` (`ensure_owner_only_dir_refuses_a_symlink_at_the_target_path`) — an in-crate unit test, not an e2e `#[spec]`-tagged catalog scenario, so there is no `#[spec]` delta for this gate to see.
