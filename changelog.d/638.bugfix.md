Fixes an interactive `codex` pane getting stuck showing "Thinking" forever: the wrapper's Codex line classifier only knew how to resolve back to `Idle` from JSON (`codex exec --json`) output, so the interactive TUI's plain-text redraw lines could drive the card to `Working` but never back down, even once the session had gone quiet. The non-JSON fallback now recognizes the interactive TUI's own composer idle placeholders ("Ask Codex to do anything", "Ask a follow-up question") as a real idle signal.
