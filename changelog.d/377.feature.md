- **The active tab is now cued by bold alone, no underline**

  The active tab in the tab bar previously carried both an underline and bold weight. The underline is dropped; the active tab is now marked with bold text only, and the whole tab name still renders in its status colour exactly as before.
