Fixes a `codex` pane wrapped through `dot-agent-deck wrap` resolving to a stale or unrelated binary via `$PATH` on a fork install where the running executable is not literally named `dot-agent-deck` (this fork's installed name is `worker-agent-deck`, CLAUDE.md rule 21) — the wrap rewrite now names this build's own resolvable binary regardless of its file name, instead of falling straight through to a bare `dot-agent-deck` name a login shell's independent `$PATH` lookup could resolve to a different, unrelated install (the reported case: six releases of upstream drift). The idempotency guard that stops the deck from double-wrapping its own output is updated to match: it now recognizes whatever this build actually resolves to, in addition to the literal `dot-agent-deck` name still used by hand-written role commands, so a Codex pane launches with exactly one wrapper layer instead of two nested ones. The residual bare-name fallback (reached only when this build's own binary can't be resolved at all) now logs a warning instead of failing silently.
