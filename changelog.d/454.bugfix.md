## Corrected two stale/false claims in the fork-sync audit doc

`docs/develop/fork-sync-workflow.md` still said PR #268's content was `DROPPED` after PR #450 had already partially restored it, and separately claimed a PRD file was deleted when it had actually been archived to `prds/done/` — a false claim that had already caused a real duplicate-PRD defect once, caught in review but never corrected in the doc itself. Both are now accurate.

No-Test: pure documentation correction, no code or test-observable behavior changed.
